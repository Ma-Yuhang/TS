export default {};

// 数组类型的扩展
// 在相同的位置上类型和值必须以一一对应
let tuple: [string, number, boolean] = ['a', 1, true];
let tuple2: [string, number, boolean, '哈哈'] = ['a', 1, true, '哈哈'];

let arr: [string[], number[], boolean] = [['ad'], [1, 2], true];
